[VALUTA]
#MODESCLUSIVA
	#STAMPA MET_DEL_CONSCOMM;1;6000004
	#IF $(RIFCOMMCLI)<>
		#MESSAGGIO 4;CONFERMI CANCELLAZIONE ?
		#IF $(RISPOSTAMESSAGGIO)==6
			#DBCONNETTI #0
				#DBESEGUISQL #0;DELETE FROM TABASSEGNAZIONICONSCOMM WHERE RIFCOMMCLI='$(RIFCOMMCLI)'
				#DBESEGUISQL #0;DELETE FROM CCBILANCIOCONSCOSTIDIRDOC WHERE IDCOMMCLI IN (SELECT PROGRESSIVO FROM ANAGRAFICACOMMESSE WHERE RIFCOMM='$(RIFCOMMCLI)')
				#DBESEGUISQL #0;DELETE FROM CCBILANCIOCONSCOSTIDIRPN WHERE IDCOMMCLI IN (SELECT PROGRESSIVO FROM ANAGRAFICACOMMESSE WHERE RIFCOMM='$(RIFCOMMCLI)')
				#DBESEGUISQL #0;DELETE FROM CCBILANCIOCONSCOSTIMATERIALE WHERE IDCOMMCLI IN (SELECT PROGRESSIVO FROM ANAGRAFICACOMMESSE WHERE RIFCOMM='$(RIFCOMMCLI)')
				#DBESEGUISQL #0;DELETE FROM CCBILANCIOCONSLAVEST WHERE IDCOMMCLI IN (SELECT PROGRESSIVO FROM ANAGRAFICACOMMESSE WHERE RIFCOMM='$(RIFCOMMCLI)')
				#DBESEGUISQL #0;DELETE FROM CCBILANCIOCONSLAVINT WHERE IDCOMMCLI IN (SELECT PROGRESSIVO FROM ANAGRAFICACOMMESSE WHERE RIFCOMM='$(RIFCOMMCLI)')
				#DBESEGUISQL #0;DELETE FROM CCBILANCIOCONSLAVINTDOC WHERE IDCOMMCLI IN (SELECT PROGRESSIVO FROM ANAGRAFICACOMMESSE WHERE RIFCOMM='$(RIFCOMMCLI)')
				#DBESEGUISQL #0;DELETE FROM CCBILANCIOMATERIALE WHERE IDCOMMCLI IN (SELECT PROGRESSIVO FROM ANAGRAFICACOMMESSE WHERE RIFCOMM='$(RIFCOMMCLI)')
			#DBDISCONNETTI #0
			#MESSAGGIO 0;ELABORAZIONE TERMINATA!!!
		#ELSE
				#MESSAGGIO 0;ELABORAZIONE ANNULLATA !
		#ENDIF
	#ELSE
		#MESSAGGIO 0;ELABORAZIONE ANNULLATA.
	#ENDIF
#FINEMODESCLUSIVA