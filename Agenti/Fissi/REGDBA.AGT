//progressivo della distinta - $(PARAMETRO:0)
//codice della distinta - $(PARAMETRO:1)
//versione della distinta - $(PARAMETRO:2)

[VALUTA]
#DBCONNETTI #0
#DBAPRITAB #0;#1;SELECT * FROM ANAGRAFICAARTICOLI WHERE CODICE='$(PARAMETRO:1)' AND AGGIORNAMAG=0
	#IF $(FINETABELLA)=1
	#ELSE
		#DBESEGUISQL #0;UPDATE DISTINTABASE SET SEQASSEMBLAGGIO='$(PARAMETRO:1)' WHERE RIFPROGRESSIVO={$(PARAMETRO:0)}
	#ENDIF

//Impostazione data validità a 01/01/2008
#LET V_ProgDBA={$(PARAMETRO:0)}
#DISABILITAVALUTAZIONE 1
#DBESEGUISQL #0;UPDATE DistintaArtComposti SET ValiditaDBA={d '2008-01-01'} WHERE Progressivo=$(V_ProgDBA)
#DISABILITAVALUTAZIONE 0

#DBCHIUDITAB #1
#DBDISCONNETTI #0
