[VALUTA]

#STAMPA SEL_WIPDEL;1
//#MESSAGGIO 0;-$(PARAMETRO)-
#IF $(PARAMETRO)<>
	#MESSAGGIO 4;CONFERMI CANCELLAZIONE ?
	#IF $(RISPOSTAMESSAGGIO)==6
		#DBCONNETTI #0
		#DBESEGUISQL #0;DELETE TESTA_WIP WHERE $(PARAMETRO)
		#DBESEGUISQL #0;DELETE RIGHE_WIP WHERE $(PARAMETRO)
		#MESSAGGIO 0;CANCELLAZIONE ESEGUITA !
		#DBDISCONNETTI #0
	#ELSE
		#MESSAGGIO 0;ELABORAZIONE ANNULLATA !
	#ENDIF
#ELSE
	#MESSAGGIO 0;ELABORAZIONE ANNULLATA.
#ENDIF