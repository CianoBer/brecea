[VALUTA]
#STAMPA AGG_PREZZI_HDP;1;111111
#DISABILITAVALUTAZIONE 1
#IF $(PARAMETRO)==
#ELSE
	#DBCONNETTI #0
		#DBESEGUISQL #0;UPDATE RIGHEDOCUMENTI SET PREZZOUNITLORDOEURO=MET_TABCATSTAT_CDC.VALORE FROM TESTEDOCUMENTI INNER JOIN RIGHEDOCUMENTI ON TESTEDOCUMENTI.PROGRESSIVO=RIGHEDOCUMENTI.IDTESTA INNER JOIN MET_TABCATSTAT_CDC ON RIGHEDOCUMENTI.CONTOCDC=CAST(MET_TABCATSTAT_CDC.CODICE AS VARCHAR) WHERE $(PARAMETRO)
		#DBESEGUISQL #0;UPDATE RIGHEDOCUMENTI SET PREZZOUNITNETTO=MET_TABCATSTAT_CDC.VALORE FROM TESTEDOCUMENTI INNER JOIN RIGHEDOCUMENTI ON TESTEDOCUMENTI.PROGRESSIVO=RIGHEDOCUMENTI.IDTESTA INNER JOIN MET_TABCATSTAT_CDC ON RIGHEDOCUMENTI.CONTOCDC=CAST(MET_TABCATSTAT_CDC.CODICE AS VARCHAR) WHERE $(PARAMETRO)
		#DBESEGUISQL #0;UPDATE RIGHEDOCUMENTI SET PREZZOUNITNETTOEURO=MET_TABCATSTAT_CDC.VALORE FROM TESTEDOCUMENTI INNER JOIN RIGHEDOCUMENTI ON TESTEDOCUMENTI.PROGRESSIVO=RIGHEDOCUMENTI.IDTESTA INNER JOIN MET_TABCATSTAT_CDC ON RIGHEDOCUMENTI.CONTOCDC=CAST(MET_TABCATSTAT_CDC.CODICE AS VARCHAR) WHERE $(PARAMETRO)
		#DBESEGUISQL #0;UPDATE RIGHEDOCUMENTI SET TOTLORDORIGA=MET_TABCATSTAT_CDC.VALORE*RIGHEDOCUMENTI.QTAGEST FROM TESTEDOCUMENTI INNER JOIN RIGHEDOCUMENTI ON TESTEDOCUMENTI.PROGRESSIVO=RIGHEDOCUMENTI.IDTESTA INNER JOIN MET_TABCATSTAT_CDC ON RIGHEDOCUMENTI.CONTOCDC=CAST(MET_TABCATSTAT_CDC.CODICE AS VARCHAR) WHERE $(PARAMETRO)
		#DBESEGUISQL #0;UPDATE RIGHEDOCUMENTI SET TOTLORDORIGARES=MET_TABCATSTAT_CDC.VALORE*RIGHEDOCUMENTI.QTAGESTRES FROM TESTEDOCUMENTI INNER JOIN RIGHEDOCUMENTI ON TESTEDOCUMENTI.PROGRESSIVO=RIGHEDOCUMENTI.IDTESTA INNER JOIN MET_TABCATSTAT_CDC ON RIGHEDOCUMENTI.CONTOCDC=CAST(MET_TABCATSTAT_CDC.CODICE AS VARCHAR) WHERE $(PARAMETRO)
		#DBESEGUISQL #0;UPDATE RIGHEDOCUMENTI SET TOTLORDORIGAEURO=MET_TABCATSTAT_CDC.VALORE*RIGHEDOCUMENTI.QTAGEST FROM TESTEDOCUMENTI INNER JOIN RIGHEDOCUMENTI ON TESTEDOCUMENTI.PROGRESSIVO=RIGHEDOCUMENTI.IDTESTA INNER JOIN MET_TABCATSTAT_CDC ON RIGHEDOCUMENTI.CONTOCDC=CAST(MET_TABCATSTAT_CDC.CODICE AS VARCHAR) WHERE $(PARAMETRO)
		#DBESEGUISQL #0;UPDATE RIGHEDOCUMENTI SET TOTLORDORIGAEURORES=MET_TABCATSTAT_CDC.VALORE*RIGHEDOCUMENTI.QTAGESTRES FROM TESTEDOCUMENTI INNER JOIN RIGHEDOCUMENTI ON TESTEDOCUMENTI.PROGRESSIVO=RIGHEDOCUMENTI.IDTESTA INNER JOIN MET_TABCATSTAT_CDC ON RIGHEDOCUMENTI.CONTOCDC=CAST(MET_TABCATSTAT_CDC.CODICE AS VARCHAR) WHERE $(PARAMETRO)
		#DBESEGUISQL #0;UPDATE RIGHEDOCUMENTI SET TOTNETTORIGA=MET_TABCATSTAT_CDC.VALORE*RIGHEDOCUMENTI.QTAGEST FROM TESTEDOCUMENTI INNER JOIN RIGHEDOCUMENTI ON TESTEDOCUMENTI.PROGRESSIVO=RIGHEDOCUMENTI.IDTESTA INNER JOIN MET_TABCATSTAT_CDC ON RIGHEDOCUMENTI.CONTOCDC=CAST(MET_TABCATSTAT_CDC.CODICE AS VARCHAR) WHERE $(PARAMETRO)
		#DBESEGUISQL #0;UPDATE RIGHEDOCUMENTI SET TOTNETTORIGARES=MET_TABCATSTAT_CDC.VALORE*RIGHEDOCUMENTI.QTAGESTRES FROM TESTEDOCUMENTI INNER JOIN RIGHEDOCUMENTI ON TESTEDOCUMENTI.PROGRESSIVO=RIGHEDOCUMENTI.IDTESTA INNER JOIN MET_TABCATSTAT_CDC ON RIGHEDOCUMENTI.CONTOCDC=CAST(MET_TABCATSTAT_CDC.CODICE AS VARCHAR) WHERE $(PARAMETRO)
		#DBESEGUISQL #0;UPDATE RIGHEDOCUMENTI SET TOTNETTORIGAEURO=MET_TABCATSTAT_CDC.VALORE*RIGHEDOCUMENTI.QTAGEST FROM TESTEDOCUMENTI INNER JOIN RIGHEDOCUMENTI ON TESTEDOCUMENTI.PROGRESSIVO=RIGHEDOCUMENTI.IDTESTA INNER JOIN MET_TABCATSTAT_CDC ON RIGHEDOCUMENTI.CONTOCDC=CAST(MET_TABCATSTAT_CDC.CODICE AS VARCHAR) WHERE $(PARAMETRO)
		#DBESEGUISQL #0;UPDATE RIGHEDOCUMENTI SET TOTNETTORIGAEURORES=MET_TABCATSTAT_CDC.VALORE*RIGHEDOCUMENTI.QTAGESTRES FROM TESTEDOCUMENTI INNER JOIN RIGHEDOCUMENTI ON TESTEDOCUMENTI.PROGRESSIVO=RIGHEDOCUMENTI.IDTESTA INNER JOIN MET_TABCATSTAT_CDC ON RIGHEDOCUMENTI.CONTOCDC=CAST(MET_TABCATSTAT_CDC.CODICE AS VARCHAR) WHERE $(PARAMETRO)
//ALLA FINE PERCHE' ALTRIMENTI NON ESEGUE GLI SCRIPT VISTO CHE CONTROLLA PREZZO UNIT LORDO=0
		#DBESEGUISQL #0;UPDATE RIGHEDOCUMENTI SET PREZZOUNITLORDO=MET_TABCATSTAT_CDC.VALORE FROM TESTEDOCUMENTI INNER JOIN RIGHEDOCUMENTI ON TESTEDOCUMENTI.PROGRESSIVO=RIGHEDOCUMENTI.IDTESTA INNER JOIN MET_TABCATSTAT_CDC ON RIGHEDOCUMENTI.CONTOCDC=CAST(MET_TABCATSTAT_CDC.CODICE AS VARCHAR) WHERE $(PARAMETRO)
	#DBDISCONNETTI #0
#ENDIF
#DISABILITAVALUTAZIONE 0
#MESSAGGIO 0;Operazione eseguita con successo!!